@extends('layouts.app')
@section('content')<main class="auth-shell"><section class="auth-card"><h1>Potwierdź e-mail</h1><p>Link weryfikacyjny został wysłany. Po weryfikacji uzyskasz dostęp do panelu.</p>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<form method="post" action="{{ route('verification.send') }}">@csrf<button class="btn btn-lime w-100">Wyślij ponownie</button></form><form method="post" action="{{ route('logout') }}" class="mt-3">@csrf<button class="btn btn-outline-secondary w-100">Wyloguj</button></form></section></main>@endsection
